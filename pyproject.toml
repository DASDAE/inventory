[project]
name = "das-metadata-docs"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0.0",
]

[tool.uv]
package = false

[tool.typos.default.extend-identifiers]
eles = "eles"
